You may have wondered, what is that `.ftr` file type?
You are only allowed to use 2 file types with Fiftree: `.txt` and `.ftr`, both can have the exact same text inside.
Why was that name chosen? Simply, because it stands for FifTRee
